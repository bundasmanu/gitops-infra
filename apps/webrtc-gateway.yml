---

name: webrtc-gateway
repoURL: https://github.com/bundasmanu/kamailio-webrtc-gateway
chartPath: k8s/helm/webrtc-gateway

namespace: kamailio

# L4 edge: charts/nginx-sip-edge (see apps/nginx-sip-edge.yml). Kamailio Service is defined in the webrtc-gateway chart.
chart_values:
  replicaCount: 2

  image:
    repository: bundasmanu/webrtc-gateway
    tag: latest
    pullPolicy: Always

  hostNetwork: false

  secrets:
    existingSecret: webrtc-gateway-secrets

  tls:
    mountPath: /etc/kamailio/certs
    readOnly: true
    existingSecret: kamailio-tls
    secretCertKey: tls.crt
    secretKeyKey: tls.key
    secretDefaultMode: 420

  volumeMounts:
    - name: config
      mountPath: /tmp/config
