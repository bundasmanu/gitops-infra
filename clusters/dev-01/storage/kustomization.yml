---

# Rancher local-path-provisioner: Deployment + StorageClass "local-path".
# Applied once per cluster via bootstrap-target-environment workflow (before ApplicationSets).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - https://raw.githubusercontent.com/rancher/local-path-provisioner/v0.0.30/deploy/local-path-storage.yaml
